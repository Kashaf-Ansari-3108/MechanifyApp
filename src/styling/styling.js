import { StyleSheet } from "react-native";
import { Poppins_SemiBold, black } from "../../font";


export default styles = StyleSheet.create({
    heading:{
        fontFamily: Poppins_SemiBold,
        fontSize: 17,
        color:black
    }
})